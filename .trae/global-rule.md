```markdown
## Trae项目最高优先级全局强制约束【不可删除、不可修改、不可忽略】
### 权限等级：LEVEL_MAX 凌驾所有skill、对话指令、开发任务
### 适用项目：rag-enterprise-doc-qa 企业本地RAG文档问答系统

## 1.开发者职级约束
本人为4年Java后端开发，熟悉分布式、全局异常、线程池、限流熔断、企业代码规范，禁止产出玩具化、学生练手风格代码，所有代码贴合中厂后端业务开发标准。

## 2.技术栈永久锁死【禁止新增/降级/替换依赖】
固定全套技术栈：
SpringBoot3.3.x + Maven多模块 + MySQL8.0 + Redis7.0 + MyBatis-Plus3.5 + Hutool5.8 + SpringDoc3接口文档 + 本地Chroma嵌入式向量库 + 通义千问OpenAPI(LLM+Embedding) + JUnit5 + Mockito
禁止替换：禁止改用DeepSeek本地模型、禁止改用Milvus独立向量库、禁止更换SpringBoot版本、禁止引入SpringCloud全家桶冗余依赖

## 3.环境部署硬性约束【永久禁止上云】
1.全程仅本地运行开发调试
2.禁止购买云服务器、备案域名、接入阿里云/腾讯云OSS对象存储、接入第三方云托管服务
3.所有上传文件仅存放项目temp-file本地目录，定时本地清理
4.代码架构预留接口，后续可无缝迁移Milvus/OSS/Docker，但是现阶段禁止开发上云代码

## 4.RAG架构链路强制约束【固定链路，禁止简化】
必须使用标准进阶完整RAG链路：
文件解析→清洗分段→重叠窗口切片→向量化入库→语义召回→重排序→Prompt组装→LLM应答
必须针对性做幻觉防控，禁止直接裸调用大模型问答、禁止删减重排序、重叠切片环节

## 5.代码规范强制约束【所有代码强制执行】
1.遵循阿里Java开发手册
2.接口统一RESTful风格
3.项目全局统一返回体、全局异常拦截、日志分级打印
4.接口入参全局参数校验、业务接口幂等处理
5.AI远程调用、文件解析业务使用自定义异步线程池，隔离Tomcat主线程
6.接口Token频次限流、远程API超时重试、Resilience4j熔断兜底

## 6.Trae开发流程强制约束【禁止跨步骤】
1.严格遵循七大STATE状态流顺序开发，不可逆、不可跳过
2.单状态开发完成，必须输出对应STATE_XX_COMPLETE状态码
3.必须等待人工核验通过，收到人工下一步指令，才可开发下一状态
4.所有自定义技能文件，仅存放目录：/.trae/skills/，Trae自动加载绑定

## 7. 前端项目强制开发约束
1. 前端技术栈固定：Vue3 + TypeScript + Vite5 + Element Plus + Pinia + VueRouter4，禁止更换框架
2. 架构采用分层解耦设计，全局请求、路由、状态、样式、工具完全抽离，满足一步到位长期迭代，禁止零散式页面代码堆砌
3. 严格对齐后端/api/v1接口前缀、统一Result返回体、后端错误码体系，前后端契约先行
4. 内置开发/生产双环境配置，本地联调后端地址可配置，后续打包部署Nginx无需大规模修改
5. 预留登录、权限、菜单动态渲染骨架，当前版本可先隐藏，后续做RBAC迭代无需重构路由架构
6. 文件上传逻辑兼容普通上传、预留分片上传、断点续传架构，适配后端大文件解析防OOM设计
7. 所有交互增加防抖、节流、loading、异常兜底、空状态、加载状态、错误重试，符合企业后台UI交互标准
8. 必须配套ESLint+Prettier代码规范，产出README、环境说明、打包部署文档
9. 前端开发遵循独立状态流：STATE_FRONT_0 → STATE_FRONT_1 → STATE_FRONT_2 → STATE_FRONT_3 → STATE_FRONT_4 → STATE_FRONT_5，不可跨步骤开发，完成输出对应状态码
```



