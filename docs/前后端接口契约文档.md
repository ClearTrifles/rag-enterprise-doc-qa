# 前后端接口契约文档

## 1. 基础信息

### 1.1 文档版本
| 版本 | 日期 | 作者 | 说明 |
| :--- | :--- | :--- | :--- |
| 1.0 | 2024-01-15 | RAG-Enterprise-Team | 初始版本 |

### 1.2 服务地址
| 环境 | 地址 | 端口 |
| :--- | :--- | :--- |
| 开发环境 | http://localhost:8080 | 8080 |
| 生产环境 | http://localhost:8080 | 8080 |

### 1.3 接口前缀
所有API接口前缀：`/api`

---

## 2. 统一响应格式

### 2.1 成功响应
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": 1705312800000,
  "success": true
}
```

### 2.2 失败响应
```json
{
  "code": 1000,
  "message": "系统异常，请稍后重试",
  "data": null,
  "timestamp": 1705312800000,
  "success": false
}
```

### 2.3 响应字段说明
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| code | Integer | 响应码，200表示成功 |
| message | String | 响应消息 |
| data | Object | 响应数据 |
| timestamp | Long | 时间戳 |
| success | Boolean | 是否成功 |

---

## 3. 错误码对照表

| 错误码 | 说明 |
| :--- | :--- |
| 200 | 操作成功 |
| 1000 | 系统异常 |
| 1001 | 参数校验失败 |
| 1002 | 请求方法不支持 |
| 1003 | 资源不存在 |
| 1004 | 未授权访问 |
| 2001 | 文档不存在 |
| 2002 | 文档上传失败 |
| 2003 | 文档解析失败 |
| 3001 | 向量数据库异常 |
| 3002 | AI服务调用失败 |

---

## 4. API接口列表

### 4.1 健康检查接口

| API路径 | HTTP方法 | 所属Controller | 功能描述 |
| :--- | :--- | :--- | :--- |
| `/api/health` | GET | HealthController | 服务健康检查 |

**请求示例：**
```
GET /api/health
```

**成功响应：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": "OK",
  "timestamp": 1705312800000,
  "success": true
}
```

---

### 4.2 文档管理接口

| API路径 | HTTP方法 | 所属Controller | 功能描述 |
| :--- | :--- | :--- | :--- |
| `/api/documents` | POST | DocumentController | 上传文档 |
| `/api/documents` | GET | DocumentController | 查询文档列表 |
| `/api/documents/{id}` | GET | DocumentController | 查询文档详情 |
| `/api/documents/{id}/status` | PUT | DocumentController | 更新文档状态 |
| `/api/documents/{id}` | DELETE | DocumentController | 删除文档 |

#### 4.2.1 上传文档

**请求示例：**
```
POST /api/documents
Content-Type: multipart/form-data

file: <文件>
documentName: 文档名称(可选)
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| file | MultipartFile | 是 | 文档文件 |
| documentName | String | 否 | 文档名称 |

**成功响应：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "documentName": "企业架构设计文档.pdf",
    "fileName": "enterprise_architecture.pdf",
    "fileType": "pdf",
    "fileSize": 2300000,
    "status": 1,
    "uploadTime": "2024-01-15 10:30:00",
    "chunkCount": 45,
    "vectorStatus": "completed",
    "storagePath": "/uploads/documents/enterprise_architecture.pdf"
  },
  "timestamp": 1705312800000,
  "success": true
}
```

#### 4.2.2 查询文档列表

**请求示例：**
```
GET /api/documents?documentName=企业&status=1&pageNum=1&pageSize=10
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| documentName | String | 否 | 文档名称（模糊搜索） |
| status | Integer | 否 | 状态（1-启用，0-禁用） |
| fileType | String | 否 | 文件类型 |
| pageNum | Integer | 否 | 页码（默认1） |
| pageSize | Integer | 否 | 每页数量（默认10） |

**成功响应：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [
      {
        "id": 1,
        "documentName": "企业架构设计文档.pdf",
        "fileName": "enterprise_architecture.pdf",
        "fileType": "pdf",
        "fileSize": 2300000,
        "status": 1,
        "uploadTime": "2024-01-15 10:30:00",
        "chunkCount": 45,
        "vectorStatus": "completed"
      }
    ],
    "total": 50,
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1705312800000,
  "success": true
}
```

#### 4.2.3 查询文档详情

**请求示例：**
```
GET /api/documents/1
```

**路径参数：**
| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 文档ID |

**成功响应：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "documentName": "企业架构设计文档.pdf",
    "fileName": "enterprise_architecture.pdf",
    "fileType": "pdf",
    "fileSize": 2300000,
    "status": 1,
    "uploadTime": "2024-01-15 10:30:00",
    "chunkCount": 45,
    "vectorStatus": "completed",
    "storagePath": "/uploads/documents/enterprise_architecture.pdf",
    "createdAt": "2024-01-15 10:30:00",
    "updatedAt": "2024-01-15 10:30:00"
  },
  "timestamp": 1705312800000,
  "success": true
}
```

#### 4.2.4 更新文档状态

**请求示例：**
```
PUT /api/documents/1/status?status=0
```

**路径参数：**
| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 文档ID |

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| status | Integer | 是 | 状态（1-启用，0-禁用） |

**成功响应：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "documentName": "企业架构设计文档.pdf",
    "status": 0
  },
  "timestamp": 1705312800000,
  "success": true
}
```

#### 4.2.5 删除文档

**请求示例：**
```
DELETE /api/documents/1
```

**路径参数：**
| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 文档ID |

**成功响应：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": null,
  "timestamp": 1705312800000,
  "success": true
}
```

---

### 4.3 智能问答接口

| API路径 | HTTP方法 | 所属Controller | 功能描述 |
| :--- | :--- | :--- | :--- |
| `/api/qa/ask` | POST | QAController | 智能问答（模式A） |
| `/api/qa/ask/mode-c` | POST | QAController | 智能问答（模式C折中方案） |
| `/api/qa/general-ask` | POST | QAController | 通用自由问答 |
| `/api/qa/history` | GET | QAController | 问答历史 |

#### 4.3.1 智能问答（模式A：原有RAG链路）

**请求示例：**
```
POST /api/qa/ask
Content-Type: application/json

{
  "question": "什么是RAG技术？",
  "sessionId": "sess-abc123",
  "documentIds": [1, 2, 3]
}
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| question | String | 是 | 用户问题 |
| sessionId | String | 否 | 会话ID（用于多轮对话） |
| documentIds | List\<Long\> | 否 | 指定文档ID列表（为空则全库检索） |

**成功响应：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "question": "什么是RAG技术？",
    "answer": "RAG（Retrieval-Augmented Generation）是一种结合检索和生成的AI技术...",
    "sessionId": "sess-abc123",
    "sources": [
      {
        "id": 1,
        "documentId": 1,
        "documentName": "企业架构设计文档.pdf",
        "pageNumber": 15,
        "content": "RAG技术是一种将信息检索与生成式AI相结合的技术..."
      }
    ],
    "confidence": 0.85,
    "responseTime": 3500,
    "createdAt": "2024-01-15 14:30:00"
  },
  "timestamp": 1705312800000,
  "success": true
}
```

#### 4.3.2 智能问答（模式C：折中方案）

**请求示例：**
```
POST /api/qa/ask/mode-c
Content-Type: application/json

{
  "question": "今天天气怎么样？",
  "sessionId": "sess-abc123"
}
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| question | String | 是 | 用户问题 |
| sessionId | String | 否 | 会话ID（用于多轮对话） |

**场景一：检索命中知识库（RAG_ANSWER）**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "replyType": "RAG_ANSWER",
    "question": "公司年假是怎么计算的？",
    "answer": "根据公司制度，员工年假计算方法如下...",
    "referenceList": [
      {
        "documentId": 1,
        "documentName": "员工手册.pdf",
        "content": "年假计算：员工入职满1年享受5天年假...",
        "score": 0.92
      }
    ],
    "promptTip": ""
  },
  "timestamp": 1705312800000,
  "success": true
}
```

**场景二：未检索到知识库内容（NEED_CONFIRM_GENERAL）**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "replyType": "NEED_CONFIRM_GENERAL",
    "question": "今天天气怎么样？",
    "answer": "",
    "referenceList": [],
    "promptTip": "未在内部私有知识库查询到该问题相关资料，请问您是否需要AI基于通用公开知识为您解答？"
  },
  "timestamp": 1705312800000,
  "success": true
}
```

**场景三：用户确认后通用问答（GENERAL_ANSWER）**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "replyType": "GENERAL_ANSWER",
    "question": "吃了吗您？",
    "answer": "还没呢，今天比较忙。你呢？需要我帮你查询什么信息吗？",
    "referenceList": [],
    "promptTip": ""
  },
  "timestamp": 1705312800000,
  "success": true
}
```

#### 4.3.3 通用自由问答

**请求示例：**
```
POST /api/qa/general-ask
Content-Type: application/json

{
  "question": "你好，请介绍一下你自己",
  "sessionId": "sess-abc123"
}
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| question | String | 是 | 用户问题 |
| sessionId | String | 否 | 会话ID（用于多轮对话） |

**成功响应：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "replyType": "GENERAL_ANSWER",
    "question": "你好，请介绍一下你自己",
    "answer": "我是RAG智能助手，一个基于检索增强生成（RAG）技术构建的企业级文档问答助手...",
    "referenceList": [],
    "promptTip": ""
  },
  "timestamp": 1705312800000,
  "success": true
}
```

#### 4.3.4 问答历史

**请求示例：**
```
GET /api/qa/history?sessionId=sess-abc123&pageNum=1&pageSize=10
```

**请求参数：**
| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| sessionId | String | 否 | 会话ID |
| pageNum | Integer | 否 | 页码（默认1） |
| pageSize | Integer | 否 | 每页数量（默认10） |

**成功响应：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [
      {
        "id": 1,
        "question": "什么是RAG技术？",
        "answer": "RAG是一种结合检索和生成的AI技术...",
        "sessionId": "sess-abc123",
        "confidence": 0.85,
        "createdAt": "2024-01-15 14:30:00"
      }
    ],
    "total": 20,
    "pageNum": 1,
    "pageSize": 10
  },
  "timestamp": 1705312800000,
  "success": true
}
```

---

## 5. 分页结构说明

### 5.1 分页请求参数
| 参数名 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| pageNum | Integer | 1 | 页码 |
| pageSize | Integer | 10 | 每页数量 |

### 5.2 分页响应结构
```json
{
  "list": [],
  "total": 100,
  "pageNum": 1,
  "pageSize": 10,
  "totalPages": 10
}
```

---

## 6. 数据模型

### 6.1 Document（文档）
| 字段名 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | Long | 文档ID |
| documentName | String | 文档名称 |
| fileName | String | 文件名称 |
| fileType | String | 文件类型 |
| fileSize | Long | 文件大小 |
| status | Integer | 状态（1-启用，0-禁用） |
| uploadTime | String | 上传时间 |
| chunkCount | Integer | 切片数量 |
| vectorStatus | String | 向量化状态 |
| storagePath | String | 存储路径 |
| createdAt | String | 创建时间 |
| updatedAt | String | 更新时间 |

### 6.2 QAAnswer（问答答案）
| 字段名 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | Long | 答案ID |
| question | String | 用户问题 |
| answer | String | AI回答 |
| sessionId | String | 会话ID |
| sources | List\<Source\> | 引用来源 |
| confidence | Double | 置信度 |
| responseTime | Long | 响应时间（毫秒） |
| createdAt | String | 创建时间 |

### 6.3 Source（引用来源）
| 字段名 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | Long | 来源ID |
| documentId | Long | 文档ID |
| documentName | String | 文档名称 |
| pageNumber | Integer | 页码 |
| content | String | 引用内容 |

---

## 7. 前端API调用示例

### 7.1 使用Axios封装

```typescript
import axios from 'axios'

const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000
})

// 请求拦截器
apiClient.interceptors.request.use(
  config => {
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器
apiClient.interceptors.response.use(
  response => {
    const data = response.data
    if (data.success) {
      return data.data
    } else {
      throw new Error(data.message)
    }
  },
  error => {
    return Promise.reject(error)
  }
)

export default apiClient
```

### 7.2 文档管理API

```typescript
// 上传文档
export const uploadDocument = (file: File, documentName?: string) => {
  const formData = new FormData()
  formData.append('file', file)
  if (documentName) {
    formData.append('documentName', documentName)
  }
  return apiClient.post('/documents', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
}

// 查询文档列表
export const getDocumentList = (params: {
  documentName?: string
  status?: number
  fileType?: string
  pageNum?: number
  pageSize?: number
}) => {
  return apiClient.get('/documents', { params })
}

// 查询文档详情
export const getDocumentById = (id: number) => {
  return apiClient.get(`/documents/${id}`)
}

// 更新文档状态
export const updateDocumentStatus = (id: number, status: number) => {
  return apiClient.put(`/documents/${id}/status`, null, {
    params: { status }
  })
}

// 删除文档
export const deleteDocument = (id: number) => {
  return apiClient.delete(`/documents/${id}`)
}
```

### 7.3 问答API

```typescript
// 智能问答
export const askQuestion = (data: {
  question: string
  sessionId?: string
  documentIds?: number[]
}) => {
  return apiClient.post('/qa/ask', data)
}

// 问答历史
export const getQAHistory = (params: {
  sessionId?: string
  pageNum?: number
  pageSize?: number
}) => {
  return apiClient.get('/qa/history', { params })
}
```